"use strict";
const mapRouter = require('express').Router();
const { response } = require('../index');
mapRouter.get('/:id', async (request, response) => {
    const street = response.json(street);
});
module.exports = mapRouter;
